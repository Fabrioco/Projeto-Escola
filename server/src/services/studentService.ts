import { AuthServiceProps } from "../interfaces/auth";

class AuthServices {
  signIn({ email, password }: AuthServiceProps) {
    try {
        
    } catch (error) {}
  }
}

export default AuthServices;
